
import { millisToDate } from 'ember-app/helpers/millis-to-date';
import { module, test } from 'qunit';

module('Unit | Helper | millis to date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = millisToDate([42]);
  assert.ok(result);
});

