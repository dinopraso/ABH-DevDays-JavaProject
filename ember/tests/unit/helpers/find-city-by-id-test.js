
import { findCityById } from 'ember-app/helpers/find-city-by-id';
import { module, test } from 'qunit';

module('Unit | Helper | find city by id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findCityById([42]);
  assert.ok(result);
});

