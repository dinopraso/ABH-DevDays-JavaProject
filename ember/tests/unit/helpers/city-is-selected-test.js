
import { cityIsSelected } from 'ember-app/helpers/city-is-selected';
import { module, test } from 'qunit';

module('Unit | Helper | city is selected');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cityIsSelected([42]);
  assert.ok(result);
});

