
import { restaurantTableEnum } from 'ember-app/helpers/restaurant-table-enum';
import { module, test } from 'qunit';

module('Unit | Helper | restaurant table enum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restaurantTableEnum([42]);
  assert.ok(result);
});

